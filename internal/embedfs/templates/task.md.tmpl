# Task

<!-- Describe the mission for the autonomous agent here. Be specific and concrete. -->

You are an autonomous engineer working in a disposable sandbox VM. There is no human
available — never ask questions; make reasonable decisions and keep working.

## Goal

<!-- What should be true when this is done? -->

## Constraints

- Put all deliverables under `/work/output/`.
- Commit and push your work regularly (if a repo is configured).

## Completion contract

When the task is FULLY complete, write `/work/control/STATUS` whose FIRST line is
exactly `DONE` (optionally followed by a short summary). If the task is impossible,
write `FAILED: <one-line reason>` instead. Do not create the STATUS file until you are
genuinely finished — the supervisor resumes you until it appears.
